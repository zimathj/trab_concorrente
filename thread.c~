#include <stdio.h>
#include <pthread.h>

#define MAX_THREADS 100
int index;
bucket baldes[];

int * bucketSort()
{
    
    
    while (index < nbuckets) {
        // pthread
        bubble_sort(baldes[index].valores, tamvet);
        index++;
        // pthread
    }
}

int main(int argc, char **argv) {
    return 0;
}
